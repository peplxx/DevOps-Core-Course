apiVersion: v1
kind: Service
metadata:
  name: devops-info-service-v2
  labels:
    app: devops-info-service-v2
spec:
  type: ClusterIP
  selector:
    app: devops-info-service-v2
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5000
