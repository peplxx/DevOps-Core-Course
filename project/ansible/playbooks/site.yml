---
# Complete infrastructure setup: provision + deploy

- name: Complete infrastructure setup
  ansible.builtin.import_playbook: provision.yml
  tags:
    - provision

- name: Deploy application
  ansible.builtin.import_playbook: deploy.yml
  tags:
    - deploy
