services:
  devops-info-service:
    build: .
    ports:
      - "5000:5000"
    environment:
      HOST: "0.0.0.0"
      PORT: "5000"
      VISITS_FILE: "/data/visits"
    volumes:
      - ./data:/data
